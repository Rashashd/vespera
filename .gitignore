CLAUDE.md
.claude/
.specify/
memory/
docs/design-system.md
docs/AI-IMPLEMENTATION-PITFALLS.md
docs/design_handoff_vespera/
scripts/dev/
docs/n8n/


# Secrets — never commit real secret values (all real secrets live in Vault)
.env
.env.*
!.env.example

# Local-only Vault provisioning helper (kept out of the repo intentionally)
scripts/write_secrets.py

# Local-only demo seed (kept on disk, out of the repo)
scripts/seed_demo.sql

# Local-only Compose port-remap override (e.g. when the host already runs Postgres/Redis)
docker-compose.override.yml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# uv
.uv/

# Jupyter
**/.ipynb_checkpoints/
*.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# local dev server logs
vite-dev*.log
