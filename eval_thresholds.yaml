classifier:
  metric: macro_f1
  min: 0.80
rag:
  hit_at_5: 0.85
  mrr: 0.70
  corroboration_accuracy: 0.75
triage:
  recall_min: 0.90
  precision_min: 0.75
agent:
  tool_selection_accuracy_min: 0.85
  report_grounding_min: 0.90
scheduling:
  due_selection_precision_min: 1.0
  due_selection_recall_min: 1.0
  duplicate_rate_max: 0
  loss_rate_max: 0
  transient_retry_recovery_min: 1.0
  inline_vs_durable_parity_min: 1.0
# Spec 12: security hardening gates. CI-gate-only — never loaded at runtime.
security:
  redaction_leak_max: 0  # planted PII/secret tokens that survive any egress point
  guardrail_block_rate_min: 1.0  # fraction of red-team attacks blocked (input+output rails)
  guardrail_false_refusal_max: 0  # legitimate PV control payloads wrongly blocked
